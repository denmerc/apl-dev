<UserControl x:Class="APLPromoter.UI.Wpf.Views.AnalyticEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <StackPanel>
        <TextBlock>Analytic Edit View</TextBlock>
        <TextBlock Text="{Binding}"></TextBlock>
        <TextBlock x:Name="id"></TextBlock>
        <!--<TextBlock x:Name="xModelName" Text="{Binding Model.Name, UpdateSourceTrigger=PropertyChanged}">3</TextBlock>-->
            <TextBlock x:Name="xModelName"></TextBlock>
        </StackPanel>
        <!--<ContentControl Content="{Binding CurrentStep}"></ContentControl>-->
       
    </Grid>
</UserControl>
